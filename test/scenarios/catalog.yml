# Shared scenario catalog for gh-actions-lock integration tests.
#
# Both the Ruby integration harness (test/integration/) and the Go scenario
# loader (test/scenarios/) consume this file. Each scenario describes:
#
#   name:        unique identifier (snake_case)
#   category:    grouping for matrix display
#   description: what the scenario validates
#   needs_token: true if the scenario requires a real GH_TOKEN
#   needs_stub:  true if the scenario uses a stub HTTP server
#   tags:        labels for filtering (e.g. "smoke", "security")
#   flags:       CLI flags passed to the binary
#   fixtures:    inline workflow/lockfile content (Ruby-side hydrates these)
#   expect:      expected exit code and output assertions
#
# The Ruby harness adds stub server wiring and custom assertions on top of
# what's declared here. The Go side uses these for table-driven scenario
# validation and matrix reporting.
#
# Scenarios with needs_token: true require a real GH_TOKEN and are
# skipped gracefully when no token is present.

version: 1

categories:
  - name: happy_path
    description: "Normal operation — actions resolve and pin successfully"
  - name: sso_auth
    description: "SAML SSO enforcement and authorization flows"
  - name: api_errors
    description: "GitHub API error conditions (404, 429, 5xx, timeout)"
  - name: lockfile
    description: "Lockfile loading, trust, corruption, and version handling"
  - name: workflow_parsing
    description: "Workflow file parsing edge cases"
  - name: output_modes
    description: "CLI flag combinations (--json, --no-fix, --rescan)"
  - name: multi_workflow
    description: "Cross-workflow scenarios and dependency tracking"
  - name: security
    description: "Impostor commit, lockfile forgery, reachability"
  - name: narrowing
    description: "Tag narrowing, --no-narrow, sticky precision, and version-ref nudges"
  - name: onboarding
    description: "--no-onboard gating, onboarding-required findings, and CI flag composition"
  - name: dependabot
    description: "Dependabot consumer contract — JSON findings shape, exit codes, and category/severity for --no-onboard --no-narrow --no-interactive"

scenarios:
  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║══════════════════════════════ happy_path ═══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: happy_path
    category: happy_path
    description: "Single action resolves and pins successfully"
    needs_token: true
    tags: [smoke]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: already_pinned_all_valid
    category: happy_path
    description: "Already-pinned v4 workflow re-pins on the verified fast path; sticky precision keeps the v4 dep key (no narrowing)"
    needs_token: true
    tags: [smoke]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_contains: ["'actions/checkout@v4':"]
      lockfile_comment_excludes: 'actions/checkout@v4\.\d'

  - name: multi_action_happy
    category: happy_path
    description: "Multiple actions from different orgs all resolve"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions:
            - "actions/checkout@v4"
            - "actions/setup-go@v5"
            - "actions/cache@v4"
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: composite_action_transitive
    category: happy_path
    description: "Action with transitive dependencies (create-github-app-token) resolves correctly"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/create-github-app-token@v1"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_deps_cover_indirect: true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═══════════════════════════════ sso_auth ════════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: sso_auth_failure
    category: sso_auth
    description: "SSO-gated org returns 403 with X-GitHub-SSO header"
    needs_stub: true
    tags: [smoke, stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions:
            - "actions/checkout@v4"
            - "actions/setup-go@v5"
            - "actions/cache@v4"
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"
        - "actions/checkout"
        - "actions/setup-go"
        - "actions/cache"
        - "SAML enforcement"
        - "Authorize in your web browser"
      output_excludes:
        - "resolution failed:"

  - name: sso_dedup
    category: sso_auth
    description: "11 actions from same org — SSO reason shown only once"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions:
            - "actions/checkout@v4"
            - "actions/setup-go@v5"
            - "actions/cache@v4"
            - "actions/upload-artifact@v4"
            - "actions/download-artifact@v4"
            - "actions/setup-python@v5"
        deploy.yml:
          name: Deploy
          actions:
            - "actions/download-artifact@v4"
            - "actions/setup-dotnet@v4"
            - "actions/setup-node@v4"
            - "actions/create-github-app-token@v1"
            - "actions/github-script@v7"
            - "actions/stale@v9"
    expect:
      exit: 1
      output_contains:
        - "actions/checkout"
        - "actions/stale"
        - "actions/github-script"
      custom: sso_dedup_count

  - name: sso_no_fix_single_url
    category: sso_auth
    description: "--no-fix: SSO authorization URL surfaces even in read-only mode"
    needs_stub: true
    tags: [stub]
    flags: ["--no-fix"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_contains: ["Authorize in your web browser"]

  - name: mixed_failures
    category: sso_auth
    description: "SSO + repo-not-found — different errors not deduped"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions:
            - "actions/checkout@v4"
            - "octo-org/private-action@v1"
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"
        - "actions/checkout"
        - "octo-org/private-action"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║══════════════════════════════ api_errors ═══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: api_404_not_found
    category: api_errors
    description: "Action repo does not exist — 404 surfaced cleanly"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["nonexistent-org/nonexistent-action@v1"]
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"
        - "nonexistent-org/nonexistent-action"

  - name: api_rate_limit_429
    category: api_errors
    description: "Rate limit 429 with Retry-After — retries and recovers or fails gracefully"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"
        - "429"

  - name: api_server_error_500
    category: api_errors
    description: "Server error 500 — fails gracefully"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"
        - "500"

  - name: api_unauthorized_401
    category: api_errors
    description: "Unauthorized 401 — permission denied error"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_contains:
        - "could not be resolved"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═══════════════════════════════ lockfile ════════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: fresh_first_run
    category: lockfile
    description: "No lockfile exists — created on first run"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_exists: true
      lockfile_deps_cover_direct: true

  - name: onboarded_corrupt_recovery
    category: lockfile
    description: "Corrupt YAML lockfile — user confirms delete, lockfile rewritten from scratch"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile: "{{{{invalid yaml content not parseable at all}}}}"
    input:
      - prompt: "(y/N)"
        response: "y"
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: onboarded_future_version
    category: lockfile
    description: "Lockfile from future version — refuses with upgrade hint"
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: future_version
    expect:
      exit: 2
      output_contains:
        - "upgrade"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═══════════════════════════ workflow_parsing ════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: no_workflows
    category: workflow_parsing
    description: "No workflow files found — graceful error"
    tags: [smoke, stub]
    fixtures:
      workflows: {}
    expect:
      exit: 2
      output_contains:
        - "no workflow"

  - name: no_workflows_summary
    category: workflow_parsing
    description: "No workflows to check — summary says so, not 'All 0 valid'"
    tags: [stub]
    fixtures:
      workflows: {}
    expect:
      exit: 2
      output_excludes: ["All 0"]

  - name: run_only_workflow
    category: workflow_parsing
    description: "Workflow with only run: steps (no actions) — RunOnly finding"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - run: echo hello
    expect:
      exit: 0

  - name: expression_in_uses
    category: workflow_parsing
    description: "Expression-based uses: (${{ }}) — unparseable, skipped with warning"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - uses: ${{ matrix.action }}
                  - uses: actions/checkout@v4
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: local_action_skipped
    category: workflow_parsing
    description: "Local action (./path) — entire workflow skipped"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - uses: ./my-local-action
                  - uses: actions/checkout@v4
    expect:
      exit: 0

  - name: local_action_onboarded_error
    category: workflow_parsing
    description: "Onboarded workflow adds local path action — hard error"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - uses: ./my-local-action
                  - uses: actions/checkout@v4
      lockfile_template: pinned_checkout
    expect:
      exit: 1
      output_contains: ["local path actions"]

  - name: sub_path_action
    category: workflow_parsing
    description: "Sub-path action (actions/cache/restore@v4) handled"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions:
            - "actions/cache/restore@v4"
            - "actions/cache/save@v4"
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: duplicate_action_refs
    category: workflow_parsing
    description: "Same action used twice in workflow — deduplicated"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - uses: actions/checkout@v4
                  - run: make build
                  - uses: actions/checkout@v4
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═════════════════════════════ output_modes ══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: no_fix_mode
    category: output_modes
    description: "--no-fix reports findings without writing — exits 1 when unpinned deps exist"
    needs_token: true
    flags: ["--no-fix"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1

  - name: json_output_valid
    category: output_modes
    description: "--json=valid produces well-formed JSON on stdout"
    needs_token: true
    flags: ["--no-fix", "--json=valid"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit_any: [0, 1]
      stdout_is_json: true

  - name: json_output_findings
    category: output_modes
    description: "--json=findings includes findings array"
    needs_stub: true
    tags: [stub]
    flags: ["--no-fix", "--json=findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      stdout_is_json: true
      stdout_contains: ["findings"]

  - name: rescan_mode
    category: output_modes
    description: "--rescan forces full re-verification"
    needs_token: true
    flags: ["--rescan"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: rescan_inconclusive_fails
    category: output_modes
    description: "--rescan with SSO-blocked verification — fails, no 'All valid'"
    needs_stub: true
    tags: [stub]
    flags: ["--rescan"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 1
      output_excludes: ["All", "valid"]

  - name: no_fix_json_combined
    category: output_modes
    description: "--no-fix --json combined produces JSON without modifying disk"
    needs_stub: true
    tags: [stub]
    flags: ["--no-fix", "--json"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      stdout_is_json: true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║════════════════════════════ multi_workflow ═════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: shared_action_across_workflows
    category: multi_workflow
    description: "Same action in multiple workflows — resolved once, tracked per-workflow"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4", "actions/setup-go@v5"]
        deploy.yml:
          name: Deploy
          actions: ["actions/checkout@v4", "actions/setup-node@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: many_workflows
    category: multi_workflow
    description: "5 workflows with overlapping actions"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4", "actions/setup-go@v5"]
        test.yml:
          name: Test
          actions: ["actions/checkout@v4", "actions/cache@v4"]
        lint.yml:
          name: Lint
          actions: ["actions/checkout@v4", "actions/setup-node@v4"]
        deploy.yml:
          name: Deploy
          actions: ["actions/checkout@v4", "actions/upload-artifact@v4"]
        release.yml:
          name: Release
          actions: ["actions/checkout@v4", "actions/create-release@v1"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: transitive_closure_cross_repo
    category: multi_workflow
    description: "Composite actions produce full transitive closure in lockfile — cross-repo deps and uses: chains preserved even when a shared dep appears in multiple workflows"
    needs_token: true
    tags: [real_repo]
    live_repo: nodeselector/actions-test-fixtures
    fixtures:
      delete_lockfile: true
    expect:
      exit: 1
      lockfile_golden: transitive_closure_cross_repo.lock
      lockfile_deps_cover_direct: true
      lockfile_deps_cover_indirect: true
      output_contains:
        - "transitive"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═══════════════════════════════ security ════════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: unresolved_uses_error_icon
    category: security
    description: "Unresolved actions use error severity, not warning"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_contains: ["could not be resolved"]

  - name: resolution_record_shown
    category: security
    description: "Resolution record path is displayed after pin"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      output_contains: ["Resolution record:"]

  - name: prefix_stripping
    category: security
    description: "Error reason prefix stripped — no 'resolution failed:' in output"
    needs_stub: true
    tags: [stub]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      output_excludes: ["resolution failed:"]
      output_contains: ["SAML enforcement"]

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║═══════════════════════════════ narrowing ═══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: fresh_v4_narrows_to_full
    category: narrowing
    description: "Default: v4 tag narrows to full semver (e.g. v4.2.1)"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v4\.\d+\.\d+'

  - name: fresh_full_tag_unchanged
    category: narrowing
    description: "Full semver v4.2.0 is already precise — no narrowing"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4.2.0"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v4\.2\.0'

  - name: fresh_branch_ref_narrows
    category: narrowing
    description: "Branch ref (main) on public action narrows to full semver"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@main"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v\d+\.\d+\.\d+'

  - name: fresh_branch_ref_no_narrow
    category: narrowing
    description: "--no-narrow: branch ref (main) stays as main"
    needs_token: true
    flags: ["--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@main"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'main'

  - name: onboarded_branch_ref_narrows
    category: narrowing
    description: "Onboarded workflow at @main — verified dep narrowing upgrades to full semver"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@main"]
      lockfile_template: pinned_checkout_main
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v\d+\.\d+\.\d+'

  - name: local_action_only
    category: workflow_parsing
    description: "Workflow with only local path actions (no remote refs) — skipped cleanly"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: ubuntu-latest
                steps:
                  - uses: ./.github/actions/my-action
                  - uses: ./.github/actions/other-action
    expect:
      exit: 0

  - name: self_hosted_runner_skipped
    category: workflow_parsing
    description: "Workflow with self-hosted runner label — skipped cleanly"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: [self-hosted, linux]
                steps:
                  - uses: actions/checkout@v4
    expect:
      exit: 0

  - name: self_hosted_runner_onboarded_error
    category: workflow_parsing
    description: "Onboarded workflow switches to self-hosted runner — hard error"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: [self-hosted, linux]
                steps:
                  - uses: actions/checkout@v4
      lockfile_template: pinned_checkout
    expect:
      exit: 1
      output_contains: ["non-hosted runner labels"]

  - name: custom_runner_label_skipped
    category: workflow_parsing
    description: "Workflow with custom runner label — skipped cleanly"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on: my-custom-runner
                steps:
                  - uses: actions/checkout@v4
    expect:
      exit: 0

  - name: runner_group_skipped
    category: workflow_parsing
    description: "Workflow with runner group — skipped cleanly"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          raw: |
            name: CI
            on: push
            jobs:
              build:
                runs-on:
                  group: my-runner-group
                  labels: [linux]
                steps:
                  - uses: actions/checkout@v4
    expect:
      exit: 0

  - name: fresh_no_narrow_keeps_major
    category: narrowing
    description: "--no-narrow: splat ref v4 stays v4 in lockfile (not narrowed to v4.x.y)"
    needs_token: true
    flags: ["--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_excludes: 'v4\.\d+\.\d+'

  - name: fresh_no_narrow_full_tag
    category: narrowing
    description: "--no-narrow: full semver v4.2.0 passes through unchanged (already precise)"
    needs_token: true
    flags: ["--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4.2.0"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v4\.2\.0'

  - name: onboarded_sticky_imprecise
    category: narrowing
    description: "Lockfile has splat ref v4 from prior --no-narrow run — re-pin without --no-narrow still keeps v4 (sticky precision)"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout_v4_imprecise
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_excludes: 'v4\.\d+\.\d+'

  - name: onboarded_full_tag_unchanged
    category: narrowing
    description: "Workflow and lockfile both say v4.2.0 — re-run is a no-op, full semver precision is preserved"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4.2.0"]
      lockfile_template: pinned_checkout_full
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_matches: 'v4\.2\.0'

  - name: onboarded_no_narrow_keeps_major
    category: narrowing
    description: "--no-narrow on onboarded workflow — splat ref v4 stays v4 in lockfile, semver nudge suppressed"
    needs_token: true
    flags: ["--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_comment_excludes: 'v4\.\d+\.\d+'
      output_excludes: ["pinned without a full semver tag"]

  - name: fresh_no_narrow_nudge_suppressed
    category: narrowing
    description: "--no-narrow: version-ref nudge is suppressed"
    needs_token: true
    flags: ["--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      output_excludes: ["Consider using full semver"]

  - name: fresh_no_fix_no_narrow_json_version_ref
    category: narrowing
    description: "--no-fix --no-narrow --json=findings: reports not-pinned finding, no version-ref noise"
    needs_token: true
    flags: ["--no-narrow", "--no-fix", "--json=findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 1
      stdout_is_json: true
      stdout_excludes: ["version-ref"]
      jq:
        - expr: '.findings | length'
          equals: 1
        - expr: '.findings[0].category'
          equals: 'not-pinned'
        - expr: '.findings[0].dependency'
          contains: 'actions/checkout'

  - name: transitive_ref_preserved
    category: narrowing
    description: "Transitive dep keeps the composite's declared ref (v1) — not narrowed, no version-ref nudge, and bare-SHA refs get a discovered tag in the ref field"
    needs_token: true
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["cli/gh-extension-precompile@v2.1.0"]
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_deps_cover_indirect: true
      lockfile_contains:
        - "'actions/attest-build-provenance@v1':"
        - "ref: 'v1'"
      lockfile_comment_excludes: 'actions/attest-build-provenance@v1\.\d+\.\d+'
      output_excludes: ["pinned without a full semver tag"]

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║══════════════════════════════ onboarding ═══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝

  - name: fresh_no_onboard_refused
    category: onboarding
    description: "--no-onboard: new (not-yet-tracked) workflow refused — exit 0 (non-blocking skip)"
    needs_token: true
    flags: ["--no-onboard"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      output_contains: ["onboarding-required"]

  - name: onboarded_no_onboard_repin
    category: onboarding
    description: "--no-onboard: already-tracked workflow re-pins normally, keeping sticky v4 precision"
    needs_token: true
    flags: ["--no-onboard"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_contains: ["'actions/checkout@v4':"]
      lockfile_comment_excludes: 'actions/checkout@v4\.\d'

  - name: onboarded_no_onboard_mixed
    category: onboarding
    description: "--no-onboard: mix of tracked and untracked — tracked re-pinned (sticky v4), untracked refused"
    needs_token: true
    flags: ["--no-onboard"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
        deploy.yml:
          name: Deploy
          actions: ["actions/setup-node@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_contains: ["'actions/checkout@v4':"]
      lockfile_comment_excludes: 'actions/checkout@v4\.\d'

  - name: fresh_no_fix_no_onboard_json_findings
    category: onboarding
    description: "--no-onboard --json=findings: refused entries appear as onboarding-required"
    needs_token: true
    flags: ["--no-onboard", "--no-fix", "--json=findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      stdout_is_json: true
      stdout_contains: ["onboarding-required"]

  - name: fresh_no_onboard_caution
    category: onboarding
    description: "--no-onboard: terminal shows caution about skipped entries"
    needs_token: true
    flags: ["--no-onboard"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      output_contains: ["onboarding-required", "skipped"]

  - name: onboarded_no_narrow_no_onboard_combined
    category: onboarding
    description: "--no-onboard --no-narrow: both flags compose — tracked re-pins at imprecise ref"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout_v4_imprecise
    expect:
      exit: 0
      lockfile_deps_cover_direct: true

  - name: fresh_no_interactive_no_onboard_ci
    category: onboarding
    description: "--no-onboard --no-interactive: CI-safe flags, no prompts"
    needs_token: true
    flags: ["--no-onboard", "--no-interactive"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0

  # ═══════════════════════════════════════════════════════════════════════
  # CATEGORY: lockfile (additional scenarios)
  # ═══════════════════════════════════════════════════════════════════════

  - name: onboarded_no_fix_corrupt
    category: lockfile
    description: "Corrupt lockfile with --no-fix — fails, no recovery attempted"
    needs_token: true
    flags: ["--no-fix"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile: "{{{{invalid yaml content not parseable at all}}}}"
    expect:
      exit: 2
      output_contains:
        - "unreadable"

  - name: onboarded_no_interactive_corrupt
    category: lockfile
    description: "Corrupt lockfile with --no-interactive — fails without prompt"
    needs_token: true
    flags: ["--no-interactive"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile: "{{{{invalid yaml content not parseable at all}}}}"
    expect:
      exit: 2
      output_contains:
        - "unreadable"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║══════════════════════════════ dependabot ═══════════════════════════════║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  # These scenarios test the contract surface consumed by dependabot-core's
  # CliEngine (cli_engine.rb). Every scenario runs with the dependabot flag
  # set: --no-onboard --no-narrow --no-interactive --json=valid,findings,workflows
  #
  # The jq assertions verify the JSON shape that dependabot-core's
  # FindingMapper relies on. Changes here must be coordinated with
  # dependabot/dependabot-core github_actions ecosystem.

  - name: dbot_onboarded_clean
    category: dependabot
    description: "All pinned, no findings — exit 0, valid true, empty findings"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--json=valid,findings,workflows"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      stdout_is_json: true
      jq:
        - expr: ".valid"
          equals: "true"
        - expr: ".findings | length"
          equals: "0"
        - expr: ".workflows | length"
          equals: "1"
        - expr: ".workflows[0].path"
          contains: "ci.yml"
        - expr: ".workflows[0].valid"
          equals: "true"

      golden_json:
        cli_version: (devel)
        findings: []
        lockfile_version: v0.0.2
        valid: true
        workflows:
          - dependencies:
              - direct: true
                hash_algo: sha1
                nwo: actions/checkout
                ref: v4
                sha: de0fac2e4500dabe0009e67214ff5f5447ce83dd
            findings: []
            path: .github/workflows/ci.yml
            valid: true
  - name: dbot_fresh_refused
    category: dependabot
    description: "Fresh repo with --no-onboard — exit 0, onboarding-required/info finding (non-blocking skip)"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--json=valid,findings,workflows"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
    expect:
      exit: 0
      stdout_is_json: true
      jq:
        - expr: ".valid"
          equals: "true"
        - expr: ".findings | length"
          greater_than: 0
        - expr: ".findings[0].category"
          equals: "onboarding-required"
        - expr: ".findings[0].severity"
          equals: "info"
        - expr: ".findings[0].workflow"
          contains: "ci.yml"

      golden_json:
        cli_version: (devel)
        findings:
          - category: onboarding-required
            confidence: high
            dependency: actions/checkout@v4
            detail: actions/checkout@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
            doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
            remediation: onboard it first with `gh actions-lock` (without --no-onboard)
            severity: info
            workflow: .github/workflows/ci.yml
        lockfile_version: v0.0.2
        valid: true
        workflows:
          - findings:
              - category: onboarding-required
                confidence: high
                dependency: actions/checkout@v4
                detail: actions/checkout@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
                doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
                remediation: onboard it first with `gh actions-lock` (without --no-onboard)
                severity: info
                workflow: .github/workflows/ci.yml
            path: .github/workflows/ci.yml
            valid: true
  - name: dbot_not_pinned
    category: dependabot
    description: "Onboarded workflow with new action — onboarding-required under --no-onboard"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--no-fix", "--json=valid,findings,workflows"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4", "actions/setup-node@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      stdout_is_json: true
      jq:
        - expr: ".valid"
          equals: "true"
        - expr: '.findings[] | select(.category == "onboarding-required") | .severity'
          equals: "info"
        - expr: '.findings[] | select(.category == "onboarding-required") | .dependency'
          contains: "actions/setup-node"

      golden_json:
        cli_version: (devel)
        findings:
          - category: onboarding-required
            confidence: high
            dependency: actions/setup-node@v4
            detail: actions/setup-node@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
            doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
            remediation: onboard it first with `gh actions-lock` (without --no-onboard)
            severity: info
            workflow: .github/workflows/ci.yml
        lockfile_version: v0.0.2
        valid: true
        workflows:
          - dependencies:
              - direct: true
                hash_algo: sha1
                nwo: actions/checkout
                ref: v4
                sha: de0fac2e4500dabe0009e67214ff5f5447ce83dd
            findings:
              - category: onboarding-required
                confidence: high
                dependency: actions/setup-node@v4
                detail: actions/setup-node@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
                doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
                remediation: onboard it first with `gh actions-lock` (without --no-onboard)
                severity: info
                workflow: .github/workflows/ci.yml
            path: .github/workflows/ci.yml
            valid: true
  - name: dbot_corrupt_lockfile_ci
    category: dependabot
    description: "Corrupt lockfile with --no-interactive — exit 2, engine error"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--json=valid,findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile: "{{{{invalid yaml content not parseable at all}}}}"
    expect:
      exit: 2
      output_contains:
        - "unreadable"

  - name: dbot_multi_workflow
    category: dependabot
    description: "Multiple workflows — per-workflow findings in JSON"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--no-fix", "--json=valid,findings,workflows"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
        deploy.yml:
          name: Deploy
          actions: ["actions/checkout@v4", "actions/setup-node@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      stdout_is_json: true
      jq:
        - expr: ".workflows | length"
          equals: "2"
        - expr: '.workflows[] | select(.path | contains("deploy")) | .valid'
          equals: "true"

      golden_json:
        cli_version: (devel)
        findings:
          - category: onboarding-required
            confidence: high
            dependency: actions/checkout@v4
            detail: actions/checkout@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
            doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
            remediation: onboard it first with `gh actions-lock` (without --no-onboard)
            severity: info
            workflow: .github/workflows/deploy.yml
          - category: onboarding-required
            confidence: high
            dependency: actions/setup-node@v4
            detail: actions/setup-node@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
            doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
            remediation: onboard it first with `gh actions-lock` (without --no-onboard)
            severity: info
            workflow: .github/workflows/deploy.yml
        lockfile_version: v0.0.2
        valid: true
        workflows:
          - dependencies:
              - direct: true
                hash_algo: sha1
                nwo: actions/checkout
                ref: v4
                sha: de0fac2e4500dabe0009e67214ff5f5447ce83dd
            findings: []
            path: .github/workflows/ci.yml
            valid: true
          - findings:
              - category: onboarding-required
                confidence: high
                dependency: actions/checkout@v4
                detail: actions/checkout@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
                doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
                remediation: onboard it first with `gh actions-lock` (without --no-onboard)
                severity: info
                workflow: .github/workflows/deploy.yml
              - category: onboarding-required
                confidence: high
                dependency: actions/setup-node@v4
                detail: actions/setup-node@v4 has no lockfile entry; --no-onboard refuses to add new workflows or actions
                doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
                remediation: onboard it first with `gh actions-lock` (without --no-onboard)
                severity: info
                workflow: .github/workflows/deploy.yml
            path: .github/workflows/deploy.yml
            valid: true
  - name: dbot_version_ref_suppressed
    category: dependabot
    description: "--no-narrow suppresses version-ref findings — not in JSON"
    needs_token: true
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--json=valid,findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      stdout_is_json: true
      jq:
        - expr: '.findings[] | select(.category == "version-ref") | .category'
          equals: ""

      golden_json:
        cli_version: (devel)
        findings: []
        lockfile_version: v0.0.2
        valid: true
  - name: dbot_transient_403_drops_pin
    category: dependabot
    description: "SSO 403 on a previously-pinned action — pin retained, clean exit"
    needs_stub: true
    tags: [stub]
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--json=valid,findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      stdout_is_json: true
      jq:
        - expr: '.valid'
          equals: "true"
        - expr: '.findings | length'
          equals: "0"

      lockfile_contains:
        - "version: 'v0.0.2'"
        - "'actions/checkout@v4':"
        - "ref: 'v4'"
        - "commit: 'sha1-de0fac2e4500dabe0009e67214ff5f5447ce83dd'"

      golden_json:
        cli_version: (devel)
        findings: []
        lockfile_version: v0.0.2
        valid: true
  - name: dbot_impostor_blocks
    category: security
    description: "Orphaned commit (no tag or branch contains it) blocks pinning"
    needs_token: true
    tags: [smoke]
    flags: ["--no-onboard", "--no-narrow", "--no-interactive"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["nodeselector/actions-test-fixtures@7b403c9ec14bd3ae0bbf793c2bee8815a7ac920b"]
    expect:
      exit: 2
      output_contains:
        - "does not exist or is not reachable"

  - name: dbot_forgery_blocks
    category: dependabot
    description: "Lockfile forgery (pin doesn't match resolved commit) produces lockfile-forgery/error finding"
    needs_stub: true
    tags: [stub]
    flags: ["--no-onboard", "--no-narrow", "--no-interactive", "--rescan", "--json=valid,findings"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: pinned_checkout
    expect:
      exit: 1
      stdout_is_json: true
      jq:
        - expr: '.valid'
          equals: "false"
        - expr: '.findings | length'
          greater_than: 0
        - expr: '.findings[] | select(.category == "lockfile-forgery") | .category'
          equals: "lockfile-forgery"
        - expr: '.findings[] | select(.category == "lockfile-forgery") | .severity'
          equals: "error"

      golden_json:
        cli_version: (devel)
        lockfile_version: v0.0.2
        valid: false
        findings:
          - category: lockfile-forgery
            confidence: high
            dependency: actions/checkout@v4
            detail: "pinned de0fac2e4500 is not an ancestor of bbbbbbbbbbbb \u2014 lockfile may have been tampered with"
            doc_url: "https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions#using-third-party-actions"
            remediation: "investigate immediately \u2014 verify the lockfile entry against upstream history"
            severity: error
            workflow: .github/workflows/ci.yml

      lockfile_contains:
        - "'actions/checkout@v4':"
        - "ref: 'v4'"
        - "commit: 'sha1-"

  - name: accept_moved_resolves_forgery
    category: security
    description: "--accept-moved re-resolves a lockfile-forgery dep to the current live SHA"
    needs_token: true
    tags: [smoke]
    flags: ["--accept-moved", "--no-narrow"]
    fixtures:
      workflows:
        ci.yml:
          name: CI
          actions: ["actions/checkout@v4"]
      lockfile_template: forgery_stale_checkout
    expect:
      exit: 0
      lockfile_deps_cover_direct: true
      lockfile_contains:
        - "'actions/checkout@v4':"
        - "ref: 'v4'"
        - "commit: 'sha1-"

  - name: onboard_roundtrip_v002
    category: onboarding
    description: "Fresh onboard writes a v0.0.2 lockfile with correct key format, ref field, and commit field"
    needs_token: true
    tags: [real_repo]
    live_repo: nodeselector/actions-test-fixtures
    fixtures:
      delete_lockfile: true
    expect:
      exit: 1
      lockfile_exists: true
      lockfile_deps_cover_direct: true
      lockfile_deps_cover_indirect: true
      lockfile_contains:
        - "version: 'v0.0.2'"
        - "ref:"
        - "commit: 'sha1-"
        - "owner_id:"
        - "repo_id:"
      lockfile_comment_excludes: '^\s+tag:'
      lockfile_comment_matches: "ref: '"
